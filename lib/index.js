// lib/index.js

// Import các hàm hoặc class từ các file trong thư mục lib
import apiConfig from './apiConfig';
import auth from './auth';
import api from './api';

const lib = {
  apiConfig,
  auth,
  api,
};

export default lib;
